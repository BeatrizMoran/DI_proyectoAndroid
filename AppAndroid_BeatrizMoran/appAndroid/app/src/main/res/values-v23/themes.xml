<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.RegistroSeries" parent="Base.Theme.RegistroSeries">
        <!-- Color barra navegación negro -->
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- Color barra estado rojo -->
        <item name="android:statusBarColor">@color/rojo</item>

        <!-- Texto e iconos de la barra estado claros (false) -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
